<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Activity Tracker</title>
  <link rel="stylesheet" href="/css/user.css">
  <script src="/js/ui-helper.js"></script>
</head>
<body class="login-page" data-ui-help-enabled="<%= uiHelpEnabled ? 'true' : 'false' %>">
  <div class="login-container">
    <div class="login-box">
      <h1>Activity Tracker</h1>
      <h2>Login Utente</h2>

      <% if (error) { %>
        <div class="alert alert-error">
          <%= error %>
        </div>
      <% } %>

      <form method="POST" action="/user/auth/login">
        <div class="form-group">
          <label for="username">Username</label>
          <input
            type="text"
            id="username"
            name="username"
            required
            autofocus
            placeholder="Inserisci username"
            data-ui-help="Inserisci il tuo username per accedere alla dashboard attività."
            data-ui-severity="info"
          >
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            required
            placeholder="Inserisci password"
            data-ui-help="Inserisci la password associata al tuo account."
            data-ui-severity="info"
          >
        </div>

        <% if (ldapEnabled) { %>
        <div class="form-group">
          <label for="authMethod">Metodo di accesso</label>
          <select id="authMethod" name="authMethod" data-ui-help="Seleziona LDAP per usare le credenziali di dominio o Locale per account locali." data-ui-severity="info">
            <option value="local" selected>Locale</option>
            <option value="ldap">LDAP</option>
          </select>
        </div>
        <% } %>

        <button type="submit" class="btn btn-primary btn-block" data-ui-help="Avvia la sessione e porta alla dashboard utente." data-ui-severity="info">
          Accedi
        </button>
      </form>

      <div class="login-footer">
        <p>Area riservata per utenti registrati</p>
        <a href="/admin/auth/login" data-ui-help="Apri la pagina di login amministratore." data-ui-severity="info">Accesso Admin</a>
      </div>
    </div>
  </div>
</body>
</html>
