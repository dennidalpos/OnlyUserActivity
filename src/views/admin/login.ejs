<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Activity Tracker</title>
  <link rel="stylesheet" href="/css/shared.css">
  <link rel="stylesheet" href="/css/admin.css">
  <script src="/js/ui-helper.js"></script>
</head>
<body class="login-page" data-ui-help-enabled="<%= uiHelpEnabled ? 'true' : 'false' %>">
  <div class="login-container">
    <div class="login-box">
      <h1>Activity Tracker</h1>
      <h2>Admin Login</h2>

      <% if (error) { %>
        <div class="alert alert-error"><%= error %></div>
      <% } %>

      <form method="POST" action="/admin/auth/login">
        <div class="form-group">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" required autofocus data-ui-help="Inserisci lo username amministrativo per accedere alle impostazioni." data-ui-severity="info">
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required data-ui-help="Inserisci la password admin per autenticarti." data-ui-severity="info">
        </div>

        <button type="submit" class="btn btn-primary btn-block" data-ui-help="Accedi al pannello amministrativo e alle funzioni di controllo." data-ui-severity="info">Login</button>
      </form>
    </div>
  </div>
</body>
</html>
